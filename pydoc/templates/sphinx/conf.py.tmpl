# -*- coding: utf-8 -*-

import os
import sys
import apitheme

# Project info

project = u'{{ project }}'
version = '{{ version }}'
release = version

# Theming

html_theme = 'apitheme'
html_theme_path = [apitheme.get_html_theme_path()]

# Extensions 

extensions = [
    'autoapi.extension',
]

# AutoAPI

autoapi_keep_files = False
autoapi_type = 'python'
autoapi_file_pattern = '*.py'
autoapi_ignore = ['packages*', 'vendor*']
autoapi_dirs = ['{{autoapi_dir}}']

# Standard stuff

templates_path = ['templates', '_templates', '.templates']
source_suffix = ['.rst', '.md']
master_doc = 'index'
copyright = u'2016'
